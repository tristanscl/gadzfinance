from setuptools import setup, find_packages

setup(
    name="gadzfinance",  # Remplace par le nom de ton module
    version="0.1",  # Numéro de version
    packages=find_packages(),  # Trouve automatiquement les packages dans le dossier
)
